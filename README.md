# **linux-.bashrc**

## Aliases that I am using
- **Gradle**
  - `grb` gradle clean build
  - `grr` gradle bootRun
  - `gra` gradle clean build && gradle bootRun
- **npm**
  - `npi` npm install
  - `nps` npm start
  - `npt` npm test
